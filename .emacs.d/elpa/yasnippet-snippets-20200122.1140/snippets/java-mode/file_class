# -*- mode: snippet -*-
# name: file_class
# key: class
# --
public class ${1:`(file-name-base
                    (or (buffer-file-name)
                        (buffer-name)))`} { 

public ${1:`(file-name-base
              (or (buffer-file-name)
                  (buffer-name)))`}($2) { 
$0
}

$0
}
